kind = "web"
previewPath = "/"
title = "Kangaroos Stats"
version = "1.0.0"
id = "artifacts/football-stats"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18532

[services.development]
run = "pnpm --filter @workspace/football-stats run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/football-stats", "run", "build" ]
publicDir = "artifacts/football-stats/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18532"
BASE_PATH = "/"
